RewriteEngine On

# React-приложение должно открываться и по прямым ссылкам, например /treatment.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ index.html [L]
