# Публикация star40.ru и откат

Цель: Reg.ru, https://star40.ru, базовый путь /. GitHub Actions только проверяет проект; GitHub Pages больше не публикует сайт.

## Подключение

HTTPS API ISPmanager: https://server193.hosting.reg.ru:1500/ispmgr, пользователь u3596090. TLS-проверка обязательна. Пароль и auth передавать в POST, не в URL.

Полный корень: /var/www/u3596090/data/www/star40.ru. Файловый API использует /www/star40.ru внутри аккаунта. Соответствие подтверждено панелью, Shell-клиентом и хэшем публичного index.html.

SSH/SFTP: server193.hosting.reg.ru, 31.31.196.172, порт 22. С компьютера пока тайм-аут. Подтверждённый ED25519: SHA256:T1OJXU1Kj1l/zJyoqbD957TTKmGgtelLGujOlvR0XEU.

CONNECT_HOSTING_PANEL.cmd позволяет проверить чтение через скрытое поле пароля. CHECK_HOSTING.cmd — SSH после восстановления доступности порта. .hosting.local.json исключён из Git и предназначен только для параметров, без пароля. Не записывать секреты в VITE_*, публичные файлы или журнал; не извлекать их из истории команд.

## Проверенный механизм HTTPS-публикации

Описание штатных форм получено через out=devel. Изменение выполняется с sok=ok. Проверку сертификата не отключать.

1. file — список каталога; file.edit без sok — просмотр текста. Для точных байтов: file.download с plid/elid **без out**. Проверять тип ответа, размер и SHA-256: JSON ошибки нельзя принять за архив.
2. file.new, filetype=1 — отдельный каталог вне публичного корня.
3. Загрузка: multipart POST на /mancgi/upload, поля mgrname=ispmgr, func=file.upload, type=data, plid, sok=ok; файловое поле filename. Загружать сначала в непубличный staging, скачивать обратно и сравнивать SHA-256 со сборкой.
4. file.unixattr с dname переименовывает, но отказывает при существующем имени. Не удалять действующий index.html ради переименования.
5. file.copyto поддерживает move=on и overwrite=on; dirlist — hex UTF-8 пути назначения из формы панели. На тестовых файлах проверены замена содержимого и исчезновение исходного файла. Это использовано для переключения HTML из staging на том же аккаунте. Внутреннюю реализацию системного rename панель не раскрывает; после операции проверены доступность сайта и точные байты.
6. file.pack: выбрать star40.ru из /www, filetype=tgz, remove_files=off, arcname вне публичного корня. Панель сама добавляет .tgz; задавать имя без расширения и проверять фактический результат.
7. file.delete — только точные имена собственных тестовых файлов/пустого staging после проверки содержимого. Старые assets и служебные файлы хостинга не удалять.

Проверено 20 сентября 2026: загрузка, чтение, переименование, перемещение с заменой, архивирование, удаление собственных тестов. Постоянного автоматического загрузчика в репозитории нет; временные инструменты выпуска и сессии не входят в Git.

## Выпуск 20 сентября 2026

- Исходная main: 07695b2ff397de8c6454b78e13223460ef5bdd28; fetch не обнаружил новых коммитов.
- npm ci при приёме проекта и повторный npm run check перед выпуском успешны на Node.js 24, VITE_BASE_PATH=/. Новых функций и обновлений зависимостей нет.
- Серверный .htaccess прочитан. Правила совпадают с локальными, различаются только CRLF/LF. Он сохранён побайтово; SHA-256 до и после: 9d2f1177a618204238d394b207f2aded433595666cee995694cfad8c14892cee.
- Сначала опубликованы и проверены по HTTPS assets/index-DN57Fc5O.css и assets/index-B0pKQmrY.js. Затем заменён 404.html, последним index.html. Перед переключением все четыре файла скачаны из staging и сверены с dist.
- Новый index.html: 3404 байта, SHA-256 a4beec960b9ad2bbab25d132e9d2a0b886169ae04093ba27163ae4defb3a9afb.
- Неизменённые ресурсы, документы, изображения и служебные файлы сохранены. Различия вне состава выпуска — две старые обрезанные фотографии и окончания строк части файлов — не перезаписывались.
- После публикации прошли 49 HTTP-проверок и браузерные проверки. Подробности — PROJECT_STATUS.md. Регрессий выпуска не обнаружено; откат не потребовался.
- Исходники, документация, локальные настройки, архивы и секреты в публичный каталог не загружались. Тестовые файлы и пустой staging удалены.

## Резервная копия и конкретный откат

Серверный архив: /var/www/u3596090/data/star40-release-20260920-9f41/star40-before-release.tgz.
В панели: /star40-release-20260920-9f41/star40-before-release.tgz.

Локальная копия: C:/Users/User/AppData/Local/star40/backups/20260920-9f41/star40-before-release.tgz.

Размер: 44 320 823 байта. SHA-256: cc0262e6c506d884a3947a0293ecd3ffb897d937c09ebd434f33f55d89e08064. Полностью прочитаны 342 файла, включая .htaccess, index, документы и assets. Рядом с локальным архивом сохранены backup-manifest.json, release-comparison.json и live-http-verification.json. Права серверного каталога копии — 700, архива — 600.

Быстрый откат именно этого выпуска:

1. Открыть в файловом менеджере /star40-release-20260920-9f41/rollback. Там старые 404.html и index.html.
2. Создать новый непубличный staging, скопировать туда оба файла, оставив rollback нетронутым. Проверить прежний index: 3912 байт, SHA-256 c5881443213b04f6be245e59558d340b627c5c9c7f82cfcb4ebfc38dbebf4c7c.
3. Через file.copyto с move=on, overwrite=on переместить сначала 404.html, затем index.html в /www/star40.ru. В интерфейсе — перемещение в указанный каталог с заменой. Старые assets уже сохранены, .htaccess и документы этот выпуск не менял.
4. Проверить главную, прямые адреса и старые ресурсы. Новый публичный index должен совпасть со старым хэшем. Новые assets при аварийном откате удалять не нужно.
5. При необходимости полного восстановления распаковать архив в новый каталог вне публичного корня, проверить вложенный star40.ru/, затем восстановить необходимые ресурсы и документы, HTML — последним. Не распаковывать вслепую поверх сайта и не удалять служебные файлы.
6. Записать причину и результат. После отправленного commit согласовать Git отдельным git revert и обычным push, без переписывания истории.

## Порядок следующих выпусков

Проверить status и origin/main → учесть изменения без потери локальной работы → Node.js 24, npm ci в чистой копии/при изменении lockfile → npm run check с / → проверить diff и секреты → прочитать серверную конфигурацию → полная проверенная копия → staging и хэши → ресурсы и их HTTP-проверка → HTML последним → браузерная проверка → commit → push main без force → GitHub Actions для точного SHA. Не считать выпуск завершённым при провале публикации или push.

## Аудит зависимостей

20 сентября 2026: три пакета уровня high, production-зависимости без обнаруженных уязвимостей. package.json/lockfile не менялись; npm audit fix --force не выполнялся.

| Пакет | Рекомендация аудита | Возможное влияние |
| --- | --- | --- |
| vite 8.0.13 | Аудит предлагает 8.3.0, без смены major, но вне текущего точного pin. Обновлять отдельно с проверкой. | Обход server.fs.deny через Windows-пути; утечка NTLMv2 через UNC в launch-editor. Риск инструментов разработки на Windows, особенно при доступном посторонним dev-сервере. Статический Reg.ru Vite не запускает. |
| postcss 8.5.14 | fixAvailable=true, совместимое обновление выше 8.5.22. | Чтение произвольных .map через sourceMappingURL при обработке недоверенного CSS/карт исходников в процессе сборки. На хостинге PostCSS не выполняется. |
| nanoid 3.3.12 | fixAvailable=true, совместимое обновление до 3.3.18 или новее. | Бесконечный цикл при отрицательном/нулевом size в отдельных генераторах; возможен отказ инструмента. Здесь это транзитивная dev-зависимость PostCSS, прямого использования нет. |

Источники npm audit: [Vite fs.deny](https://github.com/advisories/GHSA-fx2h-pf6j-xcff), [Vite UNC](https://github.com/advisories/GHSA-v6wh-96g9-6wx3), [PostCSS](https://github.com/advisories/GHSA-r28c-9q8g-f849), [PostCSS повторное исправление](https://github.com/advisories/GHSA-fxqj-rqcc-2cmp), [nanoid отрицательный size](https://github.com/advisories/GHSA-28wg-ghj8-5hjv), [nanoid нулевой size](https://github.com/advisories/GHSA-2v37-7h3g-55p8).
